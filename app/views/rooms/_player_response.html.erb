<div class="response" id="player_response_<%= player_response.id %>">
  <%= link_to_remote player_response.content,
    :url => { :action => 'build_conversation',
    :dialogue_line_id => player_response.id,
    :room_id => room.id } %>
</div>
